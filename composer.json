{
    "name": "sam/sync-package",
    "description": "A sync package for local to cloud database sync",
    "type": "library",
    "authors": [
        {
            "name": "Shahab Alam",
            "email": "your.email@example.com"
        }
    ],
    "autoload": {
        "psr-4": {
            "Sam\\SyncPackage\\": "src/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "php": "^8.1",
        "laravel/framework": "^10.10"
    },
    "extra": {
        "laravel": {
            "providers": [
                "Sam\\SyncPackage\\SyncServiceProvider"
            ]
        }
    }
}
